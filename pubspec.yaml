name: frevolt_leegality_plugin
description: A Flutter plugin for integrating Leegality e-signature functionality into FlutterFlow applications
version: 1.0.0
homepage: https://github.com/yourusername/frevolt_leegality_plugin

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.frevolt_leegality_plugin
        pluginClass: FrevoltLeegalityPlugin
      ios:
        pluginClass: FrevoltLeegalityPlugin